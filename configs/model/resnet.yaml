model: # vit_b_16
  _target_: timm.models.resnet50
  # drop_rate: 0.25  # Dropout probability before classifier, for training
  # drop_path_rate: 0.  # Stochastic depth drop-path rate
  # drop_block_rate: 0.  # Drop block rate

name: resnet50
autocast: True
